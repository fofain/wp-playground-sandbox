{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "features": {
    "networking": true
  },
  "login": true,
  "steps": [
    {
      "step": "installTheme",
      "themeData": {
        "resource": "url",
        "url": "https://raw.githubusercontent.com/fofain/wp-playground-sandbox/main/hello-elementor.zip"
      },
      "options": { "activate": true }
    },
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "url",
        "url": "https://raw.githubusercontent.com/fofain/wp-playground-sandbox/main/elementor.zip"
      },
      "options": { "activate": true }
    },
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "url",
        "url": "https://raw.githubusercontent.com/fofain/wp-playground-sandbox/main/elementor-pro.zip"
      },
      "options": { "activate": true }
    },
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "url",
        "url": "https://raw.githubusercontent.com/fofain/wp-playground-sandbox/main/frontend-editor.zip"
      },
      "options": { "activate": true }
    },
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "url",
        "url": "https://github.com/fofain/wp-playground-sandbox/raw/refs/heads/main/demo-pages-creator.php.zip"
      },
      "options": { "activate": true }
    },
    {
      "step": "runPHP",
      "code": "if (!get_role('restaurant_editor')) { add_role('restaurant_editor', 'Restaurant Editor', [ 'read' => true, 'edit_posts' => true, 'edit_pages' => true ]); } $user = get_user_by('login', 'admin'); if ($user) { $user->set_role('restaurant_editor'); }"
    }
  ]
}
